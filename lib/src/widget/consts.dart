import 'package:flutter/material.dart';

const dCurve = Curves.easeOutQuad;
const dAnimationDuration = Duration(milliseconds: 180);
const dDebounceDuration = Duration(milliseconds: 400);
const dClip = Clip.antiAlias;
const dPagePadding = EdgeInsets.fromLTRB(16, 16, 16, 40);
const dPadding = EdgeInsets.symmetric(horizontal: 16, vertical: 24);
